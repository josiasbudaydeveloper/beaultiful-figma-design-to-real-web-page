import { Poppins, Roboto } from "next/font/google";

export const poppins = Poppins({
  weight: "700",
  subsets: ["latin"]
});

export const roboto = Roboto({
  weight: "400",
  subsets: ["latin"]
});